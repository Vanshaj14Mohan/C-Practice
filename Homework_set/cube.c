#include<stdio.h>
int main()
{
    int n; //taking variable
    printf("Enter number:");
    scanf("%d", &n); //storing value in n 
    printf("Cube of number is: %d", (n*n*n)); //applying case (n*n*n)
    return 0;
}